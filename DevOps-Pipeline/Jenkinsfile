pipeline {
    agent any

    environment {
		DOCKERHUB_CREDENTIALS=credentials('dockerhub')
	}
    stages {
        // stage('Docker Login') {
        //     steps {
        //         sh 'echo $DOCKERHUB_CREDENTIALS_PSW | docker login -u $DOCKERHUB_CREDENTIALS_USR --password-stdin'
        //     }
        // }
        // stage('Build & push Dockerfile') {
        //     steps {
        //         sh """
        //         cd DevOps-Pipeline/
        //         ansible-playbook playbook.yml
        //         """
        //     }
        // }
        stage('Run Dockercompose') {
            steps {
                sh """
                cd DevOps-Pipeline/
                ansible-playbook playbook-dcompose.yml 
                """
            }
        }
        stage('Composer Update') {
            steps {
                sh """
                docker exec -it devops-pipeline_php-apache-environment_1 bash
                composer update 
                """
            }
        }
    }
}
